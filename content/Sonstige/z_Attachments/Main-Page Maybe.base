filters:
  and:
    - folderNote == true
formulas:
  Untitled: ""
  Neu: file.folder.replace(/.*\//, "")
properties:
  formula.Neu:
    displayName: Card-Name
views:
  - type: cards
    name: Karten
    filters:
      and:
        - file.folder.contains("Compass")
        - '!file.name.contains("Compass")'
        - folderNote == true
    order:
      - formula.Neu
    sort:
      - property: file.name
        direction: ASC
    imageAspectRatio: 1
    cardSize: 170
    rowHeight: medium
    image: note.Cover
  - type: table
    name: Tabelle
    filters:
      and:
        - folderNote == true
        - file.folder.contains("Compass")
        - '!file.name.contains("Compass")'
    order:
      - formula.Neu
    sort:
      - property: Folder
        direction: DESC
      - property: file.name
        direction: ASC
  - type: list
    name: Liste
    filters:
      and:
        - folderNote == true
        - '!file.name.contains("Compass")'
        - file.folder.contains("Compass")
    order:
      - formula.Neu
    sort:
      - property: file.folder
        direction: ASC
    rowHeight: medium
